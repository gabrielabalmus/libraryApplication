{
    "name": "database",
    "main": "./index.ts",
    "types": "./index.ts",
    "dependencies": {
      "@prisma/client": "^4.8.0"
    },
    "devDependencies": {
      "prisma": "^4.8.0"
    },
    "scripts": {
        "db:generate": "prisma generate",
        "db:push": "prisma db push --skip-generate"
    }
  }